# NSE Research Platform

Personal stock research system for Indian equity markets (NSE500).

## Layout

- `configs/config.yaml` is the canonical configuration file.
- `app/` holds the Python package for ingestion, indicators, scoring, backtest, sectors, reporting, and utilities.
- `docs/` holds the product and engineering specifications.
- `tests/` holds pytest coverage for the repository structure and future behavior.

